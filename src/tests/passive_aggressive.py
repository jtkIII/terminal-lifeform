from utils.utils import passive_aggressive_threshold

result = passive_aggressive_threshold(0.3)
print(f"Passive-Aggressive Threshold for aggressiveness 0.3: {result}")

result = passive_aggressive_threshold(0.7)
print(f"Passive-Aggressive Threshold for aggressiveness 0.7: {result}")

result = passive_aggressive_threshold(0.5)
print(f"Passive-Aggressive Threshold for aggressiveness 0.5: {result}")
